pipeline {
    agent any

    stages {
        stage('Example') {
            steps {
                echo "Hello World"
            }

        }

        stage('Example 2') {
                    steps {
                        sh "ls -lrt"
                        sh "java --version"
                    }

                }

    }

}