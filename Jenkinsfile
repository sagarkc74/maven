pipeline {
    agent any

    stages {
        stage('Example') {
            steps {
                echo "Hello World"
            }

        }

        stage('Example 2') {
                    steps {
                        bat "ls -lrt"
                        bat "java --version"
                    }

                }

    }

}